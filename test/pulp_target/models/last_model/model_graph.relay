#[version = "0.0.5"]
def @main(%input_0: Tensor[(1, 16), uint8], %dense_weights: Tensor[(8, 16), int8], %dense_bias: Tensor[(8), int32]) {
  %0 = nn.dense(%input_0, %dense_weights, units=None, out_dtype="int32");
  %1 = nn.bias_add(%0, %dense_bias, axis=-1);
  %2 = right_shift(%1, 15);
  %3 = clip(%2, a_min=0f, a_max=255f);
  cast(%3, dtype="uint8")
}
